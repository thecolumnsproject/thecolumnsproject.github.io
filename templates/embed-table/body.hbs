<div class="columns-table-container">
			<section class="columns-table-wrapper">
				<div class="columns-table">
					{{!-- {{#each rows}}
						{{> row this}}
					{{/each}} --}}
				</div>
				{{!-- {{> footer footer}} --}}
			</section>
		</div>